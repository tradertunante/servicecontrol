"use client";
export default function HotelModule() {
  return (
    <div
      style={{
        padding: 16,
        borderRadius: 16,
        border: "1px solid var(--border)",
        background: "rgba(0,0,0,0.03)",
        fontWeight: 900,
      }}
    >
      TODO: Migrar módulo de info del hotel aquí.
    </div>
  );
}